Here are all **45 JSONB columns** across **26 tables**:

**Key highlights:**

- **`chat_messages`** — 4 JSONB columns: `context_sources`, `debug_log`, `processing_metadata`, `routing_metadata`
- **`plan_blocks`** — `content` (NOT NULL) — the core block data
- **`plans`** — `source_details` (NOT NULL), `generation_metadata`
- **`plan_block_types`** — `output_schema` (NOT NULL), `age_overrides`
- **`plan_block_recipes`** — `recipe_definition` (NOT NULL), `age_overrides`
- **`gpt5_debug_logs`** — `json_schema`, `parsed_response`, `raw_response`
- **`transcribe`** — `diarized_json`, `transcript_json` — raw WhisperX output
- **`episode_chunks`** — `word_segments`, `char_time_map` — timing data
- **`lizi_run_logs`** — 5 JSONB columns for run state and results

NOT NULL constraints appear on: `plan_blocks.content`, `plan_block_types.output_schema`, `plan_block_recipes.recipe_definition`, `plans.source_details`, `role_entitlements.value`, `user_entitlements.value`, `transcript_preprocessed.position_map`.
