Here's the `episodes` table schema:

## episodes table — 15 columns

| Column | Type | Nullable | Default | Notes |
|--------|------|----------|---------|-------|
| `id` **PK** | `uuid` | NOT NULL | `gen_random_uuid()` | |
| `subscription_id` **FK** | `uuid` | NOT NULL | | → `subscriptions.id` |
| `provider_episode_id` | `text` | NOT NULL | | |
| `discovered_at` | `timestamptz` | nullable | `now()` | |
| `downloaded_at` | `timestamptz` | nullable | | |
| `status` | `text` | nullable | `'new'` | |
| `audio_url` | `text` | nullable | | |
| `file_path` | `text` | nullable | | |
| `duration_seconds` | `integer` | nullable | | |
| `duration_minutes` | `integer` | nullable | | |
| `created_at` | `timestamptz` | nullable | `now()` | |
| `updated_at` | `timestamptz` | nullable | `now()` | |
| `processing_metadata` | `jsonb` | nullable | | AI analysis results |
| `metadata_id` **FK** | `uuid` | nullable | | → `episode_metadata.id` |
| `error_message` | `text` | nullable | | |

**Key constraints:**
- `id` is the primary key with auto-generated UUID
- `subscription_id` is the only NOT NULL FK (required)
- `provider_episode_id` is NOT NULL (external ID from podcast provider)
- All other columns are nullable

**Heavy downstream usage** — 21 tables/views reference `episode_id`, plus 7 related RPC functions (`get_pipeline_episodes_v2`, `browse_episodes`, etc.).
