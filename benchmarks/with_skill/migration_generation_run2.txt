Looks like write permission needs to be granted. The migration SQL would be:

```sql
-- supabase/migrations/20260318000000_add_priority_to_plan_blocks.sql
ALTER TABLE bibleai.plan_blocks
    ADD COLUMN priority integer NOT NULL DEFAULT 0;
```

**`plan_blocks` schema summary:** 10 columns, uuid PK, FKs to `plan_days` and `plan_block_types`, ~13.8k rows.

**Migration details:**
- File: `supabase/migrations/20260318000000_add_priority_to_plan_blocks.sql`
- `NOT NULL DEFAULT 0` — safe to add to existing rows (all get `0`)
- No index needed unless you plan to sort/filter by priority frequently

To apply to staging:
```bash
supabase migration up --project-ref jugnzbfalgeyaqrhlvgr
```

Want me to create the file (once permissions allow) and/or apply it to staging?
