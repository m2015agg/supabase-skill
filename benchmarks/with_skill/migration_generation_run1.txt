Needs write permission for migrations. The migration SQL to add the column is:

```sql
ALTER TABLE bibleai.plan_blocks
    ADD COLUMN priority integer NOT NULL DEFAULT 0;
```

**Table summary from the schema lookup:**

- `plan_blocks` has 10 columns, ~13,881 rows
- The new `priority` column should be `integer NOT NULL DEFAULT 0` (consistent with the existing `block_order` integer column pattern)

To apply once you approve the file write:
```bash
supabase migration up --project-ref jugnzbfalgeyaqrhlvgr   # stage first
```

Would you like me to write the migration file to `supabase/migrations/20260318000000_add_priority_to_plan_blocks.sql`?
