Looks like write permission needs approval. Once granted, the migration will be:

```sql
-- supabase/migrations/20260318000000_add_priority_to_plan_blocks.sql
ALTER TABLE bibleai.plan_blocks
    ADD COLUMN priority integer NOT NULL DEFAULT 0;
```

**`plan_blocks` schema summary:**
- 10 existing columns (id, plan_day_id, block_type_id, block_order, target_duration_seconds, content, completed_at, created_at, updated_at, status)
- New column: `priority integer NOT NULL DEFAULT 0`

To apply after the file is created:
```bash
supabase migration up --project-ref jugnzbfalgeyaqrhlvgr  # stage
```
