No write permission granted. Here's the migration SQL to create manually:

**File:** `supabase/migrations/20260318000000_add_priority_to_plan_blocks.sql`

```sql
ALTER TABLE bibleai.plan_blocks
    ADD COLUMN priority integer NOT NULL DEFAULT 0;
```

**Current `plan_blocks` columns** (from schema snapshot):
- `id`, `plan_day_id`, `block_type_id`, `block_order`, `target_duration_seconds`, `content`, `completed_at`, `created_at`, `updated_at`, `status`

No existing `priority` column — safe to add.

**To apply to staging:**
```bash
supabase migration up --project-ref jugnzbfalgeyaqrhlvgr
```
