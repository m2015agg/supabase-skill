I need write permission to create the migration file. The `plan_blocks` table currently has these columns:

| Column | Type | Default |
|--------|------|---------|
| id | uuid | gen_random_uuid() |
| plan_day_id | uuid | |
| block_type_id | uuid | |
| block_order | integer | |
| target_duration_seconds | integer | |
| content | jsonb | |
| completed_at | timestamptz | |
| created_at | timestamptz | now() |
| updated_at | timestamptz | now() |
| status | text | 'locked' |

The migration SQL to add the `priority` column is:

```sql
ALTER TABLE bibleai.plan_blocks
  ADD COLUMN priority integer NOT NULL DEFAULT 0;
```

To create and apply it:
```bash
# Create
supabase migration new add_priority_to_plan_blocks
# Edit the new file with the ALTER TABLE above
# Apply to staging
supabase migration up --project-ref jugnzbfalgeyaqrhlvgr
```

Would you like me to create and apply the migration file? I'll need write permission to `supabase/migrations/`.
