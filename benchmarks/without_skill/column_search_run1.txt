It looks like I need permission to run MCP SQL queries. Please approve the `mcp__supabase__execute_sql` tool call when prompted, then I can fetch the results.

If you'd prefer not to grant that permission, an alternative is:

```bash
supabase-skill columns --type jsonb
```

This uses the local schema snapshot (`.supabase-schema/`) without hitting the live database — faster and no permission needed.
